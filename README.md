# inat-lookup

Looks up the iNat number for a Mycomap BC voucher, based on QR Code.

This is intended for internal use by the Mycomap BC team, to speed up data entry for processing submissions.

### Camera permissions

Go to `chrome://settings` and for "localhost" set to "Ask".
